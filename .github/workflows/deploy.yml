name: Deploy

on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Echo secret
        run: |
          echo "Using Vault password: ${{ secrets.VAULT_PASSWORD }}"